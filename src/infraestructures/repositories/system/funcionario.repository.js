module.exports = ({ estructures, sequilize }) => {
  const { funcionario } = estructures
  const findAll = (params = {}) => {
    const query = {}
    query.attributes = [

    ]
  }
  return { findAll }
}